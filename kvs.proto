syntax = "proto3";

service KeyValueStore {

    rpc Getrequest(Key) returns (Value) {}

    rpc Setrequest(KeyValue) returns (Empty) {}
}

message Key {
    string key = 1;
}

message Value {
    int32 value = 1;
}

message KeyValue {
    string key = 1;
    int32 value = 2;
}

message Empty {
    string empty = 1;
}